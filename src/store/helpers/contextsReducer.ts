// import { providers } from 'providers'
// import { cloneElement, ReactNode } from 'react'

// interface ContextsStoreProps {
//   children: ReactNode
// }

// export const ContextsStore = ({
//   children: initial
// }: ContextsStoreProps): JSX.Element => (
//   <>
//     {providers.reduce(
//       (children, parent) => cloneElement(parent, { children }),
//       initial
//     )}
//   </>
// )
